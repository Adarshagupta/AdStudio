export const MULTI_CLIP_WORKFLOW_STEPS = [
  "Character node — full look/voice bible",
  "Text node — full script or campaign brief",
  "Image node — hero frame for visual lock",
  "Video nodes chained left-to-right — segment-specific lines in each shot notes",
  "Run in order; export long-form from the Studio toolbar",
] as const;

export const MULTI_CLIP_WORKFLOW_SUMMARY =
  "Build joinable clips with consistent talent, wardrobe, and lighting — one segment per video node.";
