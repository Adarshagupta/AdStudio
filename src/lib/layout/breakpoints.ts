/** Studio Pro canvas requires a large viewport (Tailwind `lg`). */
export const STUDIO_PRO_MIN_WIDTH_PX = 1024;

export const STUDIO_PRO_MIN_WIDTH_MEDIA = `(min-width: ${STUDIO_PRO_MIN_WIDTH_PX}px)`;
